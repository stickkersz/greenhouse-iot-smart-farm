{
  "name": "greenhouse-iot-smart-farm",
  "private": true,
  "description": "Test suite for Firebase security rules (database.rules.json) — no build step, matches the dashboard's vanilla-JS approach",
  "scripts": {
    "test:rules": "firebase emulators:exec --only database \"node --test tests/**/*.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4"
  }
}
