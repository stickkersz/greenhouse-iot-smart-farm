# Sensitive — ห้าม commit เด็ดขาด
smartfarm_firmware/config.h

# macOS
.DS_Store
**/.DS_Store

# Firebase cache
.firebase/

# Claude session data
.claude/

# Node / npm (ถ้ามีในอนาคต)
node_modules/

# Firebase emulator log (สร้างทุกครั้งที่รัน npm run test:rules)
database-debug.log
firebase-debug.log

# Backup ทำมือ (git เก็บ history อยู่แล้ว)
dashboard/*.backup.html
