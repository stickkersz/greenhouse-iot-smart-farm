# คู่มือเพิ่มผู้ใช้งาน Dashboard — ทีมงาน บริษัท ปุ๋ยไวกิ้ง จำกัด

Dashboard **ไม่มีปุ่มสมัครสมาชิกเอง** โดยตั้งใจ เพราะ account ที่ login ได้ทุกตัวมีสิทธิ์สั่งเปิด/ปิด
ปั๊มน้ำ พัดลม และแก้ threshold ได้ทันที (คุมฮาร์ดแวร์จริง) — ถ้าเปิดให้สมัครเองสาธารณะจะเสี่ยงคนนอก
เข้ามาคุมอุปกรณ์ได้ ดังนั้น **Tonkla (หรือแอดมิน) ต้องสร้าง account ให้ทีละคนผ่าน Firebase Console**

---

## ขั้นตอนเพิ่มพนักงานใหม่ 1 คน

1. เข้า [Firebase Console](https://console.firebase.google.com) → เลือกโปรเจกต์ `greenhouse-iot-smart-farm`
2. ไปที่ **Authentication → Users → Add user**
3. ตั้งชื่อผู้ใช้เป็นภาษาอังกฤษล้วน ไม่มีช่องว่าง/อักขระพิเศษ (เช่น พนักงานชื่อ "สมชาย" → ใช้ `somchai`)
   แล้วกรอกอีเมลในช่อง Email เป็น **`somchai@smartfarm.local`** (ต้องมี `@smartfarm.local` ต่อท้ายเสมอ
   — นี่คือ trick ที่ dashboard ใช้แทน username จริง ไม่ใช่อีเมลใช้งานจริง)
4. ตั้งรหัสผ่านชั่วคราว (Firebase บังคับอย่างน้อย 6 ตัวอักษร) แล้วกด Add user
5. แจ้งพนักงานว่า:
   - เข้า dashboard ที่ `https://greenhouse-iot-smart-farm.web.app`
   - ช่อง "ชื่อผู้ใช้" ให้พิมพ์แค่ `somchai` (ไม่ต้องพิมพ์ `@smartfarm.local` — ระบบเติมให้เอง)
   - รหัสผ่านตามที่ตั้งไว้ในข้อ 4
6. ให้พนักงานลอง login จริง 1 ครั้งเพื่อยืนยันว่าเข้าได้

---

## ข้อควรระวัง

- **ทุก account มีสิทธิ์เท่ากันหมด** — อ่านข้อมูล + สั่งเปิด/ปิดอุปกรณ์จริงได้ทันที ระบบยังไม่มีการแบ่ง
  ระดับสิทธิ์ (role) เพราะทีมงานขนาดเล็ก จึงควรสร้าง account ให้เฉพาะคนที่ไว้ใจได้จริงเท่านั้น
- **เปลี่ยน/รีเซ็ตรหัสผ่าน** — ต้องทำผ่าน Firebase Console เท่านั้น (Authentication → Users → เลือก
  แถว → ⋮ → Reset password) ตัว dashboard เองยังไม่มีปุ่ม "เปลี่ยนรหัสผ่านเอง" ให้ผู้ใช้ทั่วไป
- **ปิด/ลบ account** เมื่อพนักงานลาออกหรือเปลี่ยนแผนก — Authentication → Users → เลือกแถว → Delete
  user (อย่าลืมทำทันทีที่มีคนออกจากทีม เพราะ account เก่ายังคุมอุปกรณ์จริงได้จนกว่าจะลบ)
- ทุก action ที่ทำผ่าน dashboard (เปิด/ปิดรีเลย์, แก้ threshold) จะบันทึกชื่อผู้ใช้ไว้ใน Action Log
  อัตโนมัติ — ใช้ตรวจสอบย้อนหลังได้ว่าใครสั่งอะไรเมื่อไหร่

---

*ดู `DAILY_OPERATIONS_CHECKLIST.md` สำหรับสิ่งที่ทีมควรเช็คระหว่างใช้งานประจำวัน*
